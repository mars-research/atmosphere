//! Per-CPU data structures.

// use crate::vmx::vmcs::Vmxon;

// use super::boot::get_bsp_initial_stack;

/*
/// A CPU.
#[repr(align(4096))]
pub struct Cpu {
    /// VNXON region of the CPU.
    vmxon: Vmxon,

    /// Numeric identifier of the CPU.
    id: usize,

    /// Stack space for the initial kernel thread.
    stack: *const [u8],
}
*/
