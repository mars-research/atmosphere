# Design

Just some random thoughts about verification design
- Usable verifiable replacement for core/alloc (probably will need certain trusted functions from core/alloc).
- Is there a way for the microkernal to gracefully shutdown userspace processes if they use too much memory?